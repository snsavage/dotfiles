- defaults:
    link:
      relink: true
    shell:
      stderr: true
      stdin: true
      stdout: true

- clean: ['~']

- shell:
  - ["./scripts/homebrew_setup.sh", "Installs and updates Homebrew.  Installs Brewfile packages."]
  - ["./scripts/git_config_setup.sh", "Ensure that git user name and email are configured"]
  - ["defaults write com.apple.Finder AppleShowAllFiles true", "Mac OS - Show all files"]
  - ["omz update", "Update Oh My Zsh"]

- link:
    ~/.gitconfig: gitconfig
    ~/.githelpers: githelpers
    ~/.gitignore: gitignore
    ~/.starship.toml: starship.toml
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc

# - shell:
#   -
#     description: Clone minpac for Vim
#     command: git clone https://github.com/k-takata/minpac.git ~/.vim/pack/minpac/opt/minpac
#     stdin: false
#     stout: true
#     quiet: false

