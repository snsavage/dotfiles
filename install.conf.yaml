- defaults:
    link:
      relink: true

- clean: ['~']

- shell:
  -
    command: ./homebrew_setup.sh
    description: "Installs and updates Homebrew.  Installs Brewfile packages."
    stderr: true
    stdin: true
    stdout: true


- link:
    ~/.gitconfig: gitconfig
    ~/.githelpers: githelpers
    ~/.gitignore: gitignore
    ~/.starship.toml: starship.toml
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc

- shell:
  -
    command: ./git_config_setup.sh
    description: "Ensure that git user name and email are configured"
    stderr: true
    stdin: true
    stdout: true

# - shell:
#   -
#     description: Clone minpac for Vim
#     command: git clone https://github.com/k-takata/minpac.git ~/.vim/pack/minpac/opt/minpac
#     stdin: false
#     stout: true
#     quiet: false
#   - defaults write com.apple.Finder AppleShowAllFiles true

